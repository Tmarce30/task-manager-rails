<h1>Tasks</h1>

<% @tasks.each do |task| %>
  <h2><%= link_to task.title, task_path(task) %></h2>
  <p><%= task.details %></p>
  <p><%= task.completed ? 'completed' : 'not done yet' %></p>
<% end %>

<%= link_to "Add a task", new_task_path %>
